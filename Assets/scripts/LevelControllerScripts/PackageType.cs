using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PackageType : MonoBehaviour {

	public enum HealthOfPackage {Regular, Weak, Strong};

	public enum TypeOfPackage {Circle, Box};

	public enum ColorOfPackage {Blue, Orange};

}
